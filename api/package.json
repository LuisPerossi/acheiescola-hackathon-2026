{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 3001",
    "setup": "wrangler d1 execute acheiescola-db --local --file=./schema.sql",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
